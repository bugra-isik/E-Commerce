/** @type {import('next').NextConfig} */
const nextConfig = {
  swcMinify: true,
};

module.exports = nextConfig;

module.exports = {
  fastRefresh: true,
};
module.exports = {
  concurrentFeatures: true,
  
};
